
// var btn = $("input.unlike-action[data-product-id='" + <%= @product.id %> + "']");

// btn.val('Like');
// btn.removeClass('unlike-action');
// btn.addClass('like-action');
// var parent = btn.parent('form');
// parent.attr('action', '/like?product_id=' + <%= @product.id %>);

var btn = $(".unlike-action[data-product-id='" + <%= @product.id %> + "']");
console.log(btn)
btn.attr('href', '/like?product_id=' + <%= @product.id %>);
btn.removeClass('unlike-action');
btn.addClass('like-action');

var likeCount = $(".like-quantity[data-product-id='" + <%= @product.id %> + "']");
likeCount.html('<%= @product.likes.count %>');